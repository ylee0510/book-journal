# Shelf. 料金体系 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Shelf. に月額¥330・年額¥980・永久¥2,980の3プランと20冊無料枠を実装し、Firebase Extension "Run Payments with Stripe" でセキュアに決済・購読管理する。

**Architecture:** Firebase Hosting に移行し GitHub Actions で自動デプロイ。Extension が Stripe Webhook 検証と購読状態の Firestore 同期を担い、カスタム Functions 2本が `users/{uid}.planStatus` を書き更新する。フロントエンドは planStatus と bookCount を読み取って制限 UI を制御し、Stripe Checkout へリダイレクトする。

**Tech Stack:** Firebase Hosting, Firebase Functions v2 (Node.js 20), Firestore Security Rules, Firebase Extension "Run Payments with Stripe", Stripe Checkout + Customer Portal, GitHub Actions, Jest

**Spec:** `docs/superpowers/specs/2026-05-20-pricing-design.md`

---

## ファイル構成

```
新規作成:
  firebase.json                              Firebase Hosting + Functions 設定
  .firebaserc                                プロジェクト ID 紐付け
  firestore.rules                            Security Rules
  functions/
    package.json                             Functions 依存 (firebase-admin, firebase-functions, jest)
    planStatusHelpers.js                     純粋関数 (determinePlanStatus, isLifetimePayment)
    index.js                                 onSubscriptionWrite, onPaymentWrite
    __tests__/
      planStatusHelpers.test.js              ユニットテスト
  .github/workflows/
    firebase-deploy.yml                      CI/CD ワークフロー

変更:
  index.html                                 Firebase モジュール拡張 + フロントエンド全変更
    - line 2411–2499: Firebase module        firebasePricing API 追加、import 追加
    - line 2503 付近: 変数宣言              userPlanStatus 変数追加
    - line 3066 付近: onAuthStateChanged     planStatus ロード追加
    - line 3092 付近: subscribe callback     ensureBookCount + updateLimitBanner 追加
    - line 3134: renderBooks()               バナー連動追加
    - line 4579: addBook()                   20冊チェック + bookCount++ 追加
    - line 4675: deleteBook()                bookCount-- 追加
    - CSS セクション                         バナー・モーダル・プランカードのスタイル追加
    - HTML セクション                        バナー div、アップグレードモーダル追加
    - My Page セクション                     プランセクション追加
```

---

## Task 1: Stripe と Firebase Extension のセットアップ（手動作業）

これはコードではなく手動の設定ステップです。すべて完了してから Task 2 以降に進んでください。

**Files:** なし（外部サービスの設定）

- [ ] **Step 1: Stripe ダッシュボードで3つの Price を作成する**

  1. https://dashboard.stripe.com にログイン
  2. Products → Add product で以下を作成:

  | 商品名 | 種別 | 金額 | 通貨 | 請求間隔 |
  |--------|------|------|------|----------|
  | Shelf. 月額プラン | recurring | 330 | JPY | monthly |
  | Shelf. 年額プラン | recurring | 980 | JPY | yearly |
  | Shelf. 永久プラン | one-time | 2980 | JPY | — |

  3. 各 Price の ID（`price_` で始まる文字列）をメモする

- [ ] **Step 2: Firebase コンソールで Hosting を有効化する**

  1. https://console.firebase.google.com → `book-journal-community` プロジェクト
  2. Hosting → Get started → 「Next」→「Continue to console」
  3. カスタムドメインは後で設定可（今は不要）

- [ ] **Step 3: Firebase Extension をインストールする**

  1. Firebase コンソール → Extensions → "Run Payments with Stripe" を検索
  2. Install → Blaze プランを確認して続行
  3. 設定値を入力:
     - **Stripe API key with restricted access**: Stripe Dashboard → API Keys → Create restricted key → `customers:write`, `checkout.sessions:write`, `subscriptions:read`, `payment_intents:read` を許可
     - **Cloud Functions location**: `us-central1`
     - **Customer data sync**: `Sync`
  4. インストール完了後、Extension の詳細ページで Webhook URL をコピー（`https://us-central1-book-journal-community.cloudfunctions.net/ext-firestore-stripe-payments-handleWebhookEvents` の形式）

- [ ] **Step 4: Stripe に Webhook を登録する**

  1. Stripe Dashboard → Developers → Webhooks → Add endpoint
  2. Endpoint URL: Step 3 でコピーした URL を貼り付け
  3. Events to listen to:
     - `customer.subscription.created`
     - `customer.subscription.updated`
     - `customer.subscription.deleted`
     - `checkout.session.completed`
     - `payment_intent.succeeded`
  4. Signing secret（`whsec_` で始まる文字列）をコピー
  5. Firebase コンソール → Extensions → "Run Payments with Stripe" → Reconfigure → `Stripe webhook secret` に貼り付けて保存

- [ ] **Step 5: GitHub Secrets を追加する**

  1. リポジトリ → Settings → Secrets and variables → Actions → New repository secret
  2. `FIREBASE_TOKEN` という名前で追加（値は `firebase login:ci` コマンドで取得）
     ```bash
     npx firebase-tools login:ci
     # ブラウザで認証後、ターミナルに表示されたトークンをコピー
     ```

- [ ] **Step 6: Stripe Price ID を手元にメモしておく**

  この先のタスクで以下の定数を使います（実際の値に置き換えてください）:
  ```
  PRICE_MONTHLY  = "price_xxxxxxxxxxxxxxxxxx"   # ¥330/月
  PRICE_ANNUAL   = "price_xxxxxxxxxxxxxxxxxx"   # ¥980/年
  PRICE_LIFETIME = "price_xxxxxxxxxxxxxxxxxx"   # ¥2,980 一回
  ```

---

## Task 2: Firebase 設定ファイル

**Files:**
- Create: `firebase.json`
- Create: `.firebaserc`
- Create: `firestore.rules`
- Modify: `.gitignore`（`functions/node_modules/` 追加）

- [ ] **Step 1: `firebase.json` を作成する**

  ```json
  {
    "hosting": {
      "public": ".",
      "ignore": [
        "firebase.json",
        ".firebaserc",
        "firestore.rules",
        ".git/**",
        ".github/**",
        "functions/**",
        "docs/**",
        "node_modules/**",
        "*.md"
      ],
      "rewrites": [
        {
          "source": "**",
          "destination": "/index.html"
        }
      ]
    },
    "functions": [
      {
        "source": "functions",
        "runtime": "nodejs20",
        "ignore": ["node_modules", ".git", "__tests__"]
      }
    ],
    "firestore": {
      "rules": "firestore.rules"
    }
  }
  ```

- [ ] **Step 2: `.firebaserc` を作成する**

  ```json
  {
    "projects": {
      "default": "book-journal-community"
    }
  }
  ```

- [ ] **Step 3: `firestore.rules` を作成する**

  ```
  rules_version = '2';
  service cloud.firestore {
    match /databases/{database}/documents {

      function userPlan(uid) {
        return get(/databases/$(database)/documents/users/$(uid)).data.get('planStatus', 'free');
      }
      function userBookCount(uid) {
        return get(/databases/$(database)/documents/users/$(uid)).data.get('bookCount', 0);
      }
      function canAddBook(uid) {
        return userPlan(uid) != 'free' || userBookCount(uid) < 20;
      }

      match /books/{bookId} {
        allow read: if true;
        allow create: if request.auth != null && canAddBook(request.auth.uid);
        allow update, delete: if request.auth != null && resource.data.uid == request.auth.uid;
      }

      match /users/{uid} {
        allow read: if true;
        allow create: if request.auth != null && request.auth.uid == uid;
        allow update: if request.auth != null && request.auth.uid == uid
                     && !request.resource.data.diff(resource.data).affectedKeys()
                          .hasAny(['planStatus']);
      }

      match /customers/{uid} {
        allow read: if request.auth != null && request.auth.uid == uid;
      }
      match /customers/{uid}/checkout_sessions/{id} {
        allow read, write: if request.auth != null && request.auth.uid == uid;
      }
      match /customers/{uid}/subscriptions/{id} {
        allow read: if request.auth != null && request.auth.uid == uid;
      }
      match /customers/{uid}/payments/{id} {
        allow read: if request.auth != null && request.auth.uid == uid;
      }

      match /shortLinks/{shortId} {
        allow read: if true;
        allow write: if request.auth != null;
      }
    }
  }
  ```

- [ ] **Step 4: `.gitignore` に追記する（既存ファイルがある場合は末尾に追記）**

  ```
  functions/node_modules/
  ```

- [ ] **Step 5: 動作確認（ローカル）**

  Firebase CLI がなければインストール:
  ```bash
  npm install -g firebase-tools
  firebase login
  ```

  設定ファイルの構文確認:
  ```bash
  firebase projects:list
  # book-journal-community が表示されることを確認
  ```

- [ ] **Step 6: コミット**

  ```bash
  git add firebase.json .firebaserc firestore.rules .gitignore
  git commit -m "chore: add Firebase Hosting and Firestore rules config"
  ```

---

## Task 3: GitHub Actions デプロイワークフロー

**Files:**
- Create: `.github/workflows/firebase-deploy.yml`

- [ ] **Step 1: ワークフローファイルを作成する**

  ```bash
  mkdir -p .github/workflows
  ```

  `.github/workflows/firebase-deploy.yml`:
  ```yaml
  name: Deploy to Firebase

  on:
    push:
      branches: [main]

  jobs:
    deploy:
      runs-on: ubuntu-latest
      permissions:
        contents: read

      steps:
        - uses: actions/checkout@v4

        - uses: actions/setup-node@v4
          with:
            node-version: '20'

        - name: Install Functions dependencies
          run: npm ci
          working-directory: functions

        - name: Install Firebase CLI
          run: npm install -g firebase-tools

        - name: Deploy Hosting, Functions, and Firestore Rules
          run: firebase deploy --only hosting,functions,firestore:rules --project book-journal-community
          env:
            FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
  ```

- [ ] **Step 2: 動作確認（GitHub Actions タブで実行を確認）**

  このステップはコードをコミットして push した後:
  ```
  GitHub → Actions タブ → "Deploy to Firebase" ワークフローが green になること
  ```

- [ ] **Step 3: コミット**

  ```bash
  git add .github/workflows/firebase-deploy.yml
  git commit -m "ci: add Firebase deploy workflow via GitHub Actions"
  ```

---

## Task 4: Functions プロジェクト初期化と純粋関数のテスト（TDD）

**Files:**
- Create: `functions/package.json`
- Create: `functions/planStatusHelpers.js`
- Create: `functions/__tests__/planStatusHelpers.test.js`

- [ ] **Step 1: `functions/package.json` を作成する**

  ```json
  {
    "name": "shelf-functions",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
      "test": "jest --testEnvironment node",
      "lint": "eslint ."
    },
    "engines": {
      "node": "20"
    },
    "dependencies": {
      "firebase-admin": "^12.0.0",
      "firebase-functions": "^4.0.0"
    },
    "devDependencies": {
      "jest": "^29.0.0"
    }
  }
  ```

- [ ] **Step 2: テストを先に書く**

  `functions/__tests__/planStatusHelpers.test.js`:
  ```javascript
  const { determinePlanStatus, isLifetimePayment } = require('../planStatusHelpers');

  describe('determinePlanStatus', () => {
    test('active monthly subscription returns monthly', () => {
      const sub = {
        status: 'active',
        items: [{ price: { recurring: { interval: 'month' } } }],
      };
      expect(determinePlanStatus(sub)).toBe('monthly');
    });

    test('active annual subscription returns annual', () => {
      const sub = {
        status: 'active',
        items: [{ price: { recurring: { interval: 'year' } } }],
      };
      expect(determinePlanStatus(sub)).toBe('annual');
    });

    test('trialing subscription uses interval', () => {
      const sub = {
        status: 'trialing',
        items: [{ price: { recurring: { interval: 'month' } } }],
      };
      expect(determinePlanStatus(sub)).toBe('monthly');
    });

    test('canceled subscription returns null', () => {
      const sub = { status: 'canceled', items: [] };
      expect(determinePlanStatus(sub)).toBeNull();
    });

    test('past_due subscription returns null', () => {
      const sub = { status: 'past_due', items: [] };
      expect(determinePlanStatus(sub)).toBeNull();
    });
  });

  describe('isLifetimePayment', () => {
    test('succeeded payment with lifetime metadata returns true', () => {
      const payment = { status: 'succeeded', metadata: { planType: 'lifetime' } };
      expect(isLifetimePayment(payment)).toBe(true);
    });

    test('succeeded payment without lifetime metadata returns false', () => {
      const payment = { status: 'succeeded', metadata: {} };
      expect(isLifetimePayment(payment)).toBe(false);
    });

    test('failed payment returns false', () => {
      const payment = { status: 'failed', metadata: { planType: 'lifetime' } };
      expect(isLifetimePayment(payment)).toBe(false);
    });

    test('payment with no metadata returns false', () => {
      const payment = { status: 'succeeded' };
      expect(isLifetimePayment(payment)).toBe(false);
    });
  });
  ```

- [ ] **Step 3: テストが失敗することを確認する**

  ```bash
  cd functions && npm install && npm test
  ```

  期待される出力:
  ```
  FAIL __tests__/planStatusHelpers.test.js
    ● Cannot find module '../planStatusHelpers'
  ```

- [ ] **Step 4: `functions/planStatusHelpers.js` を実装する**

  ```javascript
  function determinePlanStatus(sub) {
    const { status, items } = sub;
    if (status === 'active' || status === 'trialing') {
      const interval = items?.[0]?.price?.recurring?.interval;
      return interval === 'year' ? 'annual' : 'monthly';
    }
    return null;
  }

  function isLifetimePayment(payment) {
    return payment.status === 'succeeded' && payment.metadata?.planType === 'lifetime';
  }

  module.exports = { determinePlanStatus, isLifetimePayment };
  ```

- [ ] **Step 5: テストがパスすることを確認する**

  ```bash
  npm test
  ```

  期待される出力:
  ```
  PASS __tests__/planStatusHelpers.test.js
    determinePlanStatus
      ✓ active monthly subscription returns monthly
      ✓ active annual subscription returns annual
      ✓ trialing subscription uses interval
      ✓ canceled subscription returns null
      ✓ past_due subscription returns null
    isLifetimePayment
      ✓ succeeded payment with lifetime metadata returns true
      ✓ succeeded payment without lifetime metadata returns false
      ✓ failed payment returns false
      ✓ payment with no metadata returns false

  Test Suites: 1 passed
  Tests:       9 passed
  ```

- [ ] **Step 6: コミット**

  ```bash
  cd ..
  git add functions/
  git commit -m "feat: add Functions project with planStatusHelpers and tests"
  ```

---

## Task 5: `onSubscriptionWrite` と `onPaymentWrite` Functions の実装

**Files:**
- Create: `functions/index.js`

- [ ] **Step 1: `functions/index.js` を作成する**

  ```javascript
  const { onDocumentWritten } = require('firebase-functions/v2/firestore');
  const admin = require('firebase-admin');
  const { determinePlanStatus, isLifetimePayment } = require('./planStatusHelpers');

  admin.initializeApp();

  const db = admin.firestore();

  exports.onSubscriptionWrite = onDocumentWritten(
    'customers/{uid}/subscriptions/{subId}',
    async (event) => {
      const uid = event.params.uid;
      const after = event.data?.after;
      if (!after?.exists) return;

      const sub = after.data();
      const userRef = db.doc(`users/${uid}`);
      const newStatus = determinePlanStatus(sub);

      if (newStatus) {
        await userRef.set({ planStatus: newStatus }, { merge: true });
        return;
      }

      // Subscription ended — check if user still has any valid plan
      const [activeSubs, payments] = await Promise.all([
        db.collection(`customers/${uid}/subscriptions`)
          .where('status', 'in', ['active', 'trialing'])
          .get(),
        db.collection(`customers/${uid}/payments`)
          .where('status', '==', 'succeeded')
          .get(),
      ]);

      const hasLifetime = payments.docs.some((d) => isLifetimePayment(d.data()));

      if (!activeSubs.empty || hasLifetime) return;

      await userRef.set({ planStatus: 'free' }, { merge: true });
    }
  );

  exports.onPaymentWrite = onDocumentWritten(
    'customers/{uid}/payments/{paymentId}',
    async (event) => {
      const uid = event.params.uid;
      const after = event.data?.after;
      if (!after?.exists) return;

      const payment = after.data();

      if (isLifetimePayment(payment)) {
        await db.doc(`users/${uid}`).set({ planStatus: 'lifetime' }, { merge: true });
      }
    }
  );
  ```

- [ ] **Step 2: テストが引き続きパスすることを確認する**

  ```bash
  cd functions && npm test
  ```

  期待される出力: 全9テスト PASS（index.js を import するテストはないため変化なし）

- [ ] **Step 3: Functions のローカル動作確認（オプション・Firebase Emulator 使用）**

  ```bash
  # ルートに戻る
  cd ..
  npx firebase-tools emulators:start --only functions,firestore
  # Emulator UI: http://localhost:4000
  # Firestore で customers/{uid}/subscriptions/{id} に
  # { status: "active", items: [{ price: { recurring: { interval: "month" } } }] }
  # を手動で書き込み、users/{uid}.planStatus が "monthly" になることを確認
  ```

- [ ] **Step 4: コミット**

  ```bash
  git add functions/index.js
  git commit -m "feat: add onSubscriptionWrite and onPaymentWrite Cloud Functions"
  ```

---

## Task 6: `index.html` — Firebase モジュール拡張（`window.firebasePricing`）

`index.html` の Firebase module script（line 2411–2500）を修正し、決済・プラン確認 API を追加する。

**Files:**
- Modify: `index.html`（line 2411–2500 付近）

- [ ] **Step 1: Firebase module の import に3行追加する**

  既存（line 2413–2418）:
  ```javascript
  import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-app.js";
  import {
    getFirestore, collection, doc, getDoc, getDocs, setDoc, deleteDoc, onSnapshot, query, orderBy
  } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-firestore.js";
  import {
    getAuth, signInWithPopup, GoogleAuthProvider, onAuthStateChanged, signOut
  } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-auth.js";
  ```

  変更後:
  ```javascript
  import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-app.js";
  import {
    getFirestore, collection, doc, getDoc, getDocs, setDoc, deleteDoc,
    onSnapshot, query, orderBy, increment, where
  } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-firestore.js";
  import {
    getAuth, signInWithPopup, GoogleAuthProvider, onAuthStateChanged, signOut
  } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-auth.js";
  import {
    getFunctions, httpsCallable
  } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-functions.js";
  ```

- [ ] **Step 2: `window.firebaseStorage` に `updateBookCount` と `initBookCount` を追加する**

  既存の `window.firebaseStorage = { ... }` ブロック（line 2472–2495）の末尾（`};` の直前）に追記:
  ```javascript
      async updateBookCount(uid, delta) {
        await setDoc(doc(db, 'users', uid), { bookCount: increment(delta) }, { merge: true });
      },
      async initBookCount(uid, currentCount) {
        const snap = await getDoc(doc(db, 'users', uid));
        if (snap.exists() && typeof snap.data().bookCount === 'number') return;
        await setDoc(doc(db, 'users', uid), { bookCount: currentCount }, { merge: true });
      },
  ```

- [ ] **Step 3: `window.firebasePricing` を `window.firebaseReady = true;` の直前に追加する**

  ```javascript
  const firebaseFunctions = getFunctions(app, 'us-central1');

  window.firebasePricing = {
    PRICE_MONTHLY:  'price_xxxxxxxxxxxxxxxxxx',  // ← Task 1 でメモした ID に置き換える
    PRICE_ANNUAL:   'price_xxxxxxxxxxxxxxxxxx',  // ← Task 1 でメモした ID に置き換える
    PRICE_LIFETIME: 'price_xxxxxxxxxxxxxxxxxx',  // ← Task 1 でメモした ID に置き換える

    async getUserPlanStatus(uid) {
      const snap = await getDoc(doc(db, 'users', uid));
      return snap.exists() ? (snap.data().planStatus || 'free') : 'free';
    },

    async startCheckout(priceId, mode, planType) {
      const uid = auth.currentUser?.uid;
      if (!uid) throw new Error('Not authenticated');
      const sessionRef = doc(collection(db, 'customers', uid, 'checkout_sessions'));
      const sessionData = {
        price: priceId,
        mode: mode,
        success_url: window.location.origin + window.location.pathname + '?payment=success',
        cancel_url:  window.location.origin + window.location.pathname + '?payment=cancel',
      };
      if (planType === 'lifetime') {
        sessionData.metadata = { planType: 'lifetime' };
      }
      await setDoc(sessionRef, sessionData);
      return new Promise((resolve, reject) => {
        const unsub = onSnapshot(sessionRef, (snap) => {
          const data = snap.data();
          if (data?.url) {
            unsub();
            window.location.href = data.url;
            resolve();
          } else if (data?.error) {
            unsub();
            reject(new Error(data.error?.message || 'チェックアウトエラー'));
          }
        });
        setTimeout(() => { unsub(); reject(new Error('タイムアウト: もう一度お試しください')); }, 15000);
      });
    },

    async openBillingPortal() {
      const createPortalLink = httpsCallable(
        firebaseFunctions,
        'ext-firestore-stripe-payments-createPortalLink'
      );
      const { data } = await createPortalLink({
        returnUrl: window.location.origin + window.location.pathname,
      });
      window.location.href = data.url;
    },
  };
  ```

  **注意:** `PRICE_MONTHLY` / `PRICE_ANNUAL` / `PRICE_LIFETIME` の値は Task 1 でメモした実際の Stripe Price ID に必ず置き換えること。

- [ ] **Step 4: ブラウザで動作確認する**

  ```
  1. ブラウザで index.html を開く（file:// または ローカルサーバー）
  2. DevTools コンソールで以下を実行:
     window.firebasePricing
  3. オブジェクトが表示され、startCheckout / openBillingPortal などが見えること
  4. コンソールエラーがないこと（import エラーがあれば URL を確認）
  ```

- [ ] **Step 5: コミット**

  ```bash
  git add index.html
  git commit -m "feat: extend Firebase module with firebasePricing and bookCount helpers"
  ```

---

## Task 7: `index.html` — planStatus の読み込みと bookCount 管理

**Files:**
- Modify: `index.html`（main script 部分）

- [ ] **Step 1: `userPlanStatus` 変数を追加する**

  既存（line 2503 付近）:
  ```javascript
  let currentUser = null;
  let userProfile = null;
  ```

  変更後:
  ```javascript
  let currentUser = null;
  let userProfile = null;
  let userPlanStatus = 'free';
  ```

- [ ] **Step 2: `loadPlanStatus` 関数を `handleSignOut` の直後に追加する**

  ```javascript
  async function loadPlanStatus(uid) {
    if (!window.firebasePricing) return;
    userPlanStatus = await window.firebasePricing.getUserPlanStatus(uid);
    updateLimitBanner();
  }
  ```

- [ ] **Step 3: `onAuthStateChanged` コールバックを修正する**

  既存（line 3066）:
  ```javascript
  window.firebaseAuth.onAuthStateChanged((user) => {
    currentUser = user;
    updateAuthUI();
  });
  ```

  変更後:
  ```javascript
  window.firebaseAuth.onAuthStateChanged(async (user) => {
    currentUser = user;
    userPlanStatus = 'free';
    updateAuthUI();
    if (user) {
      await loadPlanStatus(user.uid);
    }
    updateLimitBanner();
  });
  ```

- [ ] **Step 4: Firestore subscribe コールバック（line 3092）にカウント初期化を追加する**

  既存:
  ```javascript
  firebaseUnsubscribe = window.firebaseStorage.subscribe((items) => {
    books = items.sort((a, b) => (b.createdAt || 0) - (a.createdAt || 0));
    renderBooks();
  ```

  変更後:
  ```javascript
  firebaseUnsubscribe = window.firebaseStorage.subscribe(async (items) => {
    books = items.sort((a, b) => (b.createdAt || 0) - (a.createdAt || 0));
    renderBooks();
    if (currentUser && window.firebaseStorage) {
      const myCount = books.filter(isMyBook).length;
      await window.firebaseStorage.initBookCount(currentUser.uid, myCount);
    }
  ```

- [ ] **Step 5: `addBook()` に20冊チェックと bookCount++ を追加する**

  既存（line 4638 付近）の `} else {` ブランチ（新規作成側）の先頭:
  ```javascript
  } else {
    // CREATE new book
    const id = ...
  ```

  変更後:
  ```javascript
  } else {
    // CREATE new book — check free plan limit
    const myBooks = books.filter(isMyBook);
    if (userPlanStatus === 'free' && myBooks.length >= 20) {
      btn.disabled = false;
      btn.textContent = '記録する';
      openUpgradeModal();
      return;
    }

    const id = Date.now().toString(36) + Math.random().toString(36).slice(2, 7);
    const book = { ... };  // 既存のまま

    try {
      await storage.set('book:' + id, JSON.stringify(book));
      // bookCount をインクリメント
      if (currentUser && window.firebaseStorage) {
        await window.firebaseStorage.updateBookCount(currentUser.uid, 1);
      }
      if (!window.firebaseStorage) {
        books.unshift(book);
        renderBooks();
      }
      closeModal();
      showToast('記録しました ✓');
    } catch (err) {
  ```

- [ ] **Step 6: `deleteBook()` に bookCount-- を追加する**

  既存（line 4675）:
  ```javascript
  async function deleteBook(event, id) {
    event.stopPropagation();
    if (!confirm('この記録を削除しますか?')) return;
    try {
      await storage.delete('book:' + id);
      if (!window.firebaseStorage) { ... }
      showToast('削除しました');
    }
  ```

  変更後（`storage.delete` の直後に追加）:
  ```javascript
      await storage.delete('book:' + id);
      if (currentUser && window.firebaseStorage) {
        await window.firebaseStorage.updateBookCount(currentUser.uid, -1);
      }
  ```

- [ ] **Step 7: ブラウザで動作確認する**

  ```
  1. Google ログインする
  2. DevTools コンソールで userPlanStatus を確認 → 'free' が表示されること
  3. コンソールに Firestore エラーがないこと
  ```

- [ ] **Step 8: コミット**

  ```bash
  git add index.html
  git commit -m "feat: load planStatus on auth and manage bookCount on add/delete"
  ```

---

## Task 8: `index.html` — 20冊到達バナーと `updateLimitBanner()`

**Files:**
- Modify: `index.html`（CSS + HTML + JS）

- [ ] **Step 1: CSS を追加する**

  既存 CSS ブロック（`<style>` 内、他スタイルの末尾付近）に追加:
  ```css
  /* ==================== LIMIT BANNER ==================== */
  .limit-banner {
    display: none;
    background: linear-gradient(135deg, var(--accent) 0%, #c9522f 100%);
    color: #fff;
    padding: 0.75rem 1.5rem;
    text-align: center;
    font-family: 'Noto Serif JP', serif;
    font-size: 0.85rem;
    position: relative;
    z-index: 10;
  }
  .limit-banner.visible { display: flex; align-items: center; justify-content: center; gap: 1rem; flex-wrap: wrap; }
  .limit-banner-text { line-height: 1.5; }
  .limit-banner-btn {
    background: rgba(255,255,255,0.2);
    border: 1px solid rgba(255,255,255,0.5);
    color: #fff;
    padding: 0.3rem 0.9rem;
    border-radius: 2rem;
    font-size: 0.8rem;
    cursor: pointer;
    white-space: nowrap;
    font-family: 'Noto Serif JP', serif;
  }
  .limit-banner-btn:hover { background: rgba(255,255,255,0.35); }
  ```

- [ ] **Step 2: HTML を追加する**

  `<main class="books-grid" id="booksGrid">` の直前（line 2127 付近）に挿入:
  ```html
  <!-- 20冊到達バナー -->
  <div class="limit-banner" id="limitBanner">
    <span class="limit-banner-text">
      読書記録20冊に到達しました — 21冊目からは有料プランが必要です
    </span>
    <button class="limit-banner-btn" onclick="showMyPage(); setTimeout(() => document.getElementById('mpPlan')?.scrollIntoView({ behavior: 'smooth' }), 200)">
      プランを見る
    </button>
  </div>
  ```

- [ ] **Step 3: `updateLimitBanner()` 関数を追加する**

  `loadPlanStatus` 関数の直後に追加:
  ```javascript
  function updateLimitBanner() {
    const banner = document.getElementById('limitBanner');
    if (!banner) return;
    const myBooks = currentUser ? books.filter(isMyBook) : [];
    const show = userPlanStatus === 'free' && myBooks.length >= 20;
    banner.classList.toggle('visible', show);
  }
  ```

- [ ] **Step 4: `renderBooks()` から `updateLimitBanner()` を呼ぶ**

  `renderBooks()` 関数（line 3134）の末尾（関数の `}` の直前）に追加:
  ```javascript
    updateLimitBanner();
  ```

- [ ] **Step 5: ブラウザで動作確認する**

  ```
  1. DevTools コンソールで以下を実行してバナーを強制表示:
     userPlanStatus = 'free';
     books = Array(20).fill({ uid: currentUser?.uid, status: 'finished' });
     updateLimitBanner();
  2. 赤いバナーが本棚の上部に表示されること
  3. 「プランを見る」ボタンクリックで My Page が開くこと（mpPlan はまだないので Task 10 で確認）
  4. userPlanStatus = 'monthly'; updateLimitBanner(); でバナーが消えること
  ```

- [ ] **Step 6: コミット**

  ```bash
  git add index.html
  git commit -m "feat: add 20-book limit banner with plan upgrade prompt"
  ```

---

## Task 9: `index.html` — アップグレードモーダル

**Files:**
- Modify: `index.html`（CSS + HTML + JS）

- [ ] **Step 1: CSS を追加する**

  ```css
  /* ==================== UPGRADE MODAL ==================== */
  .upgrade-overlay {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(26,23,20,0.6);
    z-index: 300;
    align-items: center;
    justify-content: center;
    padding: 1.5rem;
  }
  .upgrade-overlay.open { display: flex; }
  .upgrade-modal {
    background: var(--card);
    border-radius: 0.5rem;
    padding: 2rem 2rem 1.5rem;
    max-width: 560px;
    width: 100%;
    box-shadow: 0 20px 60px -20px rgba(26,23,20,0.4);
    position: relative;
  }
  .upgrade-modal-close {
    position: absolute;
    top: 1rem; right: 1.2rem;
    background: none; border: none;
    font-size: 1.4rem; color: var(--ink-fade);
    cursor: pointer; line-height: 1;
  }
  .upgrade-modal-close:hover { color: var(--ink); }
  .upgrade-modal-title {
    font-family: 'Fraunces', serif;
    font-size: 1.4rem;
    font-weight: 300;
    margin-bottom: 0.3rem;
  }
  .upgrade-modal-sub {
    font-size: 0.8rem;
    color: var(--ink-fade);
    font-family: 'JetBrains Mono', monospace;
    margin-bottom: 1.5rem;
  }
  .plan-cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.75rem;
    margin-bottom: 1.2rem;
  }
  @media (max-width: 500px) {
    .plan-cards { grid-template-columns: 1fr; }
  }
  .plan-card {
    border: 1px solid var(--line);
    border-radius: 0.4rem;
    padding: 1rem 0.8rem;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    background: var(--bg);
    position: relative;
  }
  .plan-card.recommended {
    border-color: var(--accent);
    background: #fff9f7;
  }
  .plan-card-badge {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.6rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--accent);
    font-weight: 700;
  }
  .plan-card-name {
    font-family: 'Noto Serif JP', serif;
    font-size: 0.9rem;
    font-weight: 600;
  }
  .plan-card-price {
    font-family: 'JetBrains Mono', monospace;
    font-size: 1.2rem;
    font-weight: 700;
    color: var(--ink);
  }
  .plan-card-price span {
    font-size: 0.7rem;
    font-weight: 400;
    color: var(--ink-fade);
  }
  .plan-card-note {
    font-size: 0.7rem;
    color: var(--ink-soft);
    min-height: 1.2em;
  }
  .plan-card-btn {
    margin-top: 0.5rem;
    padding: 0.5rem;
    background: var(--ink);
    color: #fff;
    border: none;
    border-radius: 0.25rem;
    font-family: 'Noto Serif JP', serif;
    font-size: 0.8rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.4rem;
    min-height: 2rem;
  }
  .plan-card-btn:hover { background: var(--accent); }
  .plan-card-btn:disabled { opacity: 0.6; cursor: not-allowed; }
  .plan-card.recommended .plan-card-btn { background: var(--accent); }
  .plan-card.recommended .plan-card-btn:hover { background: #a33c26; }
  .upgrade-modal-note {
    font-size: 0.72rem;
    color: var(--ink-fade);
    text-align: center;
    line-height: 1.5;
  }
  ```

- [ ] **Step 2: HTML を追加する**

  `<div class="toast" id="toast"></div>` の直前に追加:
  ```html
  <!-- アップグレードモーダル -->
  <div class="upgrade-overlay" id="upgradeOverlay" onclick="if(event.target===this)closeUpgradeModal()">
    <div class="upgrade-modal">
      <button class="upgrade-modal-close" onclick="closeUpgradeModal()">×</button>
      <div class="upgrade-modal-title">読書記録の上限に到達しました</div>
      <div class="upgrade-modal-sub">無料プランは20冊まで — 続けるにはアップグレードを</div>
      <div class="plan-cards">
        <div class="plan-card">
          <div class="plan-card-name">月額プラン</div>
          <div class="plan-card-price">¥330<span>/月</span></div>
          <div class="plan-card-note"></div>
          <button class="plan-card-btn" id="btnMonthly" onclick="handlePlanSelect('monthly')">
            アップグレード
          </button>
        </div>
        <div class="plan-card recommended">
          <div class="plan-card-badge">おすすめ</div>
          <div class="plan-card-name">年額プラン</div>
          <div class="plan-card-price">¥980<span>/年</span></div>
          <div class="plan-card-note">月あたり約¥81 — 73%お得</div>
          <button class="plan-card-btn" id="btnAnnual" onclick="handlePlanSelect('annual')">
            アップグレード
          </button>
        </div>
        <div class="plan-card">
          <div class="plan-card-name">永久プラン</div>
          <div class="plan-card-price">¥2,980</div>
          <div class="plan-card-note">買い切り・無期限</div>
          <button class="plan-card-btn" id="btnLifetime" onclick="handlePlanSelect('lifetime')">
            購入する
          </button>
        </div>
      </div>
      <div class="upgrade-modal-note">
        決済は Stripe の安全なページで行われます。いつでもキャンセル可能。
      </div>
    </div>
  </div>
  ```

- [ ] **Step 3: JS を追加する**

  `openUpgradeModal` / `closeUpgradeModal` / `handlePlanSelect` を main script に追加:
  ```javascript
  function openUpgradeModal() {
    document.getElementById('upgradeOverlay').classList.add('open');
  }

  function closeUpgradeModal() {
    document.getElementById('upgradeOverlay').classList.remove('open');
  }

  async function handlePlanSelect(planKey) {
    if (!currentUser) { handleSignIn(); return; }
    if (!window.firebasePricing) { showToast('決済サービスの準備中です'); return; }

    const p = window.firebasePricing;
    const configs = {
      monthly:  { priceId: p.PRICE_MONTHLY,  mode: 'subscription', planType: null },
      annual:   { priceId: p.PRICE_ANNUAL,   mode: 'subscription', planType: null },
      lifetime: { priceId: p.PRICE_LIFETIME, mode: 'payment',      planType: 'lifetime' },
    };
    const cfg = configs[planKey];
    if (!cfg) return;

    const btnId = { monthly: 'btnMonthly', annual: 'btnAnnual', lifetime: 'btnLifetime' }[planKey];
    const btn = document.getElementById(btnId);
    const originalText = btn.textContent;
    btn.disabled = true;
    btn.innerHTML = '<span style="display:inline-block;width:14px;height:14px;border:2px solid #fff;border-top-color:transparent;border-radius:50%;animation:spin 0.6s linear infinite;vertical-align:middle"></span>';

    try {
      await p.startCheckout(cfg.priceId, cfg.mode, cfg.planType);
    } catch (err) {
      showToast('エラー: ' + (err.message || '決済を開始できませんでした'));
      btn.disabled = false;
      btn.textContent = originalText;
    }
  }
  ```

- [ ] **Step 4: ブラウザで動作確認する**

  ```
  1. DevTools コンソールで openUpgradeModal() を実行
  2. モーダルが表示されること
  3. 背景クリックでモーダルが閉じること
  4. × ボタンでモーダルが閉じること
  5. モバイルサイズ（375px）でカードが縦積みになること
  ```

- [ ] **Step 5: コミット**

  ```bash
  git add index.html
  git commit -m "feat: add upgrade modal with 3 plan cards and Stripe checkout trigger"
  ```

---

## Task 10: `index.html` — My Page プランセクション

**Files:**
- Modify: `index.html`（CSS + My Page HTML テンプレート + JS）

- [ ] **Step 1: CSS を追加する**

  ```css
  /* ==================== MY PAGE PLAN SECTION ==================== */
  .mp-plan-badge {
    display: inline-block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.65rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    padding: 0.2rem 0.6rem;
    border-radius: 2rem;
    background: var(--ink-fade);
    color: #fff;
    vertical-align: middle;
  }
  .mp-plan-badge.free { background: var(--ink-fade); }
  .mp-plan-badge.monthly, .mp-plan-badge.annual { background: var(--accent); }
  .mp-plan-badge.lifetime { background: var(--gold); }
  .mp-plan-meta {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.72rem;
    color: var(--ink-fade);
    margin-top: 0.3rem;
  }
  .mp-plan-cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.6rem;
    margin-top: 1rem;
  }
  @media (max-width: 480px) {
    .mp-plan-cards { grid-template-columns: 1fr; }
  }
  .mp-portal-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    margin-top: 1rem;
    padding: 0.6rem 1.2rem;
    border: 1px solid var(--line);
    background: var(--bg);
    color: var(--ink-soft);
    font-family: 'Noto Serif JP', serif;
    font-size: 0.82rem;
    border-radius: 0.25rem;
    cursor: pointer;
  }
  .mp-portal-btn:hover { border-color: var(--ink-soft); color: var(--ink); }
  ```

- [ ] **Step 2: My Page コンテナに `mpPlan` div を追加する**

  `renderMyPage()` 関数（line 2664）内の `container.innerHTML` の `mpProfile` の前に `mpPlan` を追加:
  ```javascript
  container.innerHTML = `
    <div id="mpPlan" class="mypage-section"></div>
    <div id="mpProfile" class="mypage-section"></div>
    <div id="mpGoal" class="mypage-section"></div>
    <div id="mpShelf" class="mypage-section"></div>
    <div id="mpWishlist" class="mypage-section"></div>
  `;
  ```

  そして `await loadUserProfile(currentUser.uid);` の後に `renderMpPlan();` を追加:
  ```javascript
  await loadUserProfile(currentUser.uid);
  renderMpPlan();
  renderMpProfile();
  ...
  ```

- [ ] **Step 3: `renderMpPlan()` 関数を追加する**

  `renderMpProfile` 関数の直前に追加:
  ```javascript
  function renderMpPlan() {
    const el = document.getElementById('mpPlan');
    if (!el) return;
    const p = window.firebasePricing;
    const myBooks = books.filter(isMyBook);
    const count = myBooks.length;

    const badgeClass = { free: 'free', monthly: 'monthly', annual: 'annual', lifetime: 'lifetime' }[userPlanStatus] || 'free';
    const badgeLabel = { free: 'Free', monthly: '月額プラン', annual: '年額プラン', lifetime: '永久プラン' }[userPlanStatus] || 'Free';

    let body = '';
    if (userPlanStatus === 'free') {
      body = `
        <div class="mp-plan-meta">${count} / 20 冊使用中${count >= 20 ? ' — 上限に達しました' : ''}</div>
        <div class="mp-plan-cards">
          <div class="plan-card">
            <div class="plan-card-name">月額プラン</div>
            <div class="plan-card-price">¥330<span>/月</span></div>
            <div class="plan-card-note"></div>
            <button class="plan-card-btn" onclick="handlePlanSelect('monthly')">アップグレード</button>
          </div>
          <div class="plan-card recommended">
            <div class="plan-card-badge">おすすめ</div>
            <div class="plan-card-name">年額プラン</div>
            <div class="plan-card-price">¥980<span>/年</span></div>
            <div class="plan-card-note">月あたり約¥81 — 73%お得</div>
            <button class="plan-card-btn" onclick="handlePlanSelect('annual')">アップグレード</button>
          </div>
          <div class="plan-card">
            <div class="plan-card-name">永久プラン</div>
            <div class="plan-card-price">¥2,980</div>
            <div class="plan-card-note">買い切り・無期限</div>
            <button class="plan-card-btn" onclick="handlePlanSelect('lifetime')">購入する</button>
          </div>
        </div>`;
    } else if (userPlanStatus === 'lifetime') {
      body = `<div class="mp-plan-meta">無期限 — 何冊でも登録できます</div>`;
    } else {
      // monthly or annual — show billing portal button
      body = `
        <div class="mp-plan-meta">有効なサブスクリプション</div>
        <div>
          <button class="mp-portal-btn" onclick="handleOpenPortal()">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="5" width="20" height="14" rx="2"/><path d="M2 10h20"/></svg>
            請求を管理する
          </button>
        </div>`;
    }

    el.innerHTML = `
      <div class="mypage-section-title">Plan <span class="mp-plan-badge ${badgeClass}">${badgeLabel}</span></div>
      ${body}`;
  }

  async function handleOpenPortal() {
    if (!window.firebasePricing) return;
    try {
      showToast('Stripe へリダイレクト中...');
      await window.firebasePricing.openBillingPortal();
    } catch (err) {
      showToast('エラー: ' + (err.message || 'ポータルを開けませんでした'));
    }
  }
  ```

- [ ] **Step 4: ブラウザで動作確認する**

  ```
  1. Google ログインして My Page を開く
  2. 「Plan」セクションが最上部に表示されること
  3. [Free] バッジと「0 / 20 冊使用中」が表示されること
  4. 3つのプランカードが表示されること
  5. DevTools で userPlanStatus = 'monthly'; renderMpPlan(); → 請求管理ボタンのみ表示されること
  6. DevTools で userPlanStatus = 'lifetime'; renderMpPlan(); → 「無期限」テキストのみ表示されること
  ```

- [ ] **Step 5: コミット**

  ```bash
  git add index.html
  git commit -m "feat: add Plan section to My Page with upgrade cards and billing portal"
  ```

---

## Task 11: `index.html` — 決済後 URL ハンドリング

Stripe Checkout 完了・キャンセル後に `?payment=success` / `?payment=cancel` がついて戻ってくるため、これを処理する。

**Files:**
- Modify: `index.html`（JS、ページロード時の処理）

- [ ] **Step 1: URL ハンドリングを `loadBooks()` の呼び出し付近に追加する**

  `window.addEventListener('firebaseReady', () => { loadBooks(); });`（line 3129 付近）の後に追加:
  ```javascript
  // Handle return from Stripe Checkout
  (function handlePaymentReturn() {
    const params = new URLSearchParams(window.location.search);
    const payment = params.get('payment');
    if (!payment) return;
    // Clean the URL without reloading
    const cleanUrl = window.location.pathname;
    history.replaceState({}, '', cleanUrl);
    if (payment === 'success') {
      // Wait for Firebase auth to settle before showing toast
      setTimeout(() => {
        showToast('決済が完了しました。プランが反映されるまで少々お待ちください ✓');
        if (currentUser) loadPlanStatus(currentUser.uid);
      }, 1500);
    } else if (payment === 'cancel') {
      setTimeout(() => showToast('決済をキャンセルしました'), 500);
    }
  })();
  ```

- [ ] **Step 2: アニメーション用 `@keyframes spin` を CSS に追加する（未定義の場合）**

  CSS ブロック末尾付近に追加:
  ```css
  @keyframes spin {
    to { transform: rotate(360deg); }
  }
  ```

- [ ] **Step 3: ブラウザで動作確認する**

  ```
  1. ブラウザのアドレスバーで ?payment=success を末尾に追加してアクセス:
     例: http://localhost:xxxx/index.html?payment=success
  2. 「決済が完了しました」トーストが表示されること
  3. URL から ?payment=success が消えること（ページはリロードしない）
  4. ?payment=cancel でキャンセルトーストが表示されること
  ```

- [ ] **Step 4: コミット**

  ```bash
  git add index.html
  git commit -m "feat: handle Stripe Checkout success/cancel URL params"
  ```

---

## Task 12: デプロイと動作確認

**Files:** なし（デプロイと確認）

- [ ] **Step 1: Firestore Rules をデプロイする**

  ```bash
  firebase deploy --only firestore:rules
  ```

  期待される出力:
  ```
  ✔ firestore: released rules firestore.rules to cloud.firestore
  ```

- [ ] **Step 2: Functions をデプロイする**

  ```bash
  firebase deploy --only functions
  ```

  期待される出力:
  ```
  ✔ functions[onSubscriptionWrite]: Successful create operation.
  ✔ functions[onPaymentWrite]: Successful create operation.
  ```

- [ ] **Step 3: Hosting をデプロイする**

  ```bash
  firebase deploy --only hosting
  ```

  期待される出力:
  ```
  ✔ Hosting URL: https://book-journal-community.web.app
  ```

- [ ] **Step 4: GitHub に push して CI/CD を確認する**

  ```bash
  git push origin main
  ```

  GitHub → Actions タブで "Deploy to Firebase" ワークフローが緑になることを確認。

- [ ] **Step 5: E2E 動作確認チェックリスト**

  以下をブラウザ（https://book-journal-community.web.app）で手動確認:

  ```
  □ Google ログインが正常に動作する
  □ My Page → Plan セクションが表示され [Free] バッジが見える
  □ 本を20冊記録する（または DevTools で books を20件に見せかける）
  □ 赤い「20冊到達」バナーが表示される
  □ 21冊目を登録しようとするとアップグレードモーダルが表示される
  □ 「年額プラン」→「アップグレード」ボタンクリックで Stripe Checkout ページに遷移する
  □ テストカード 4242 4242 4242 4242 で決済を完了する（Stripe テストモード）
  □ ?payment=success つきで戻り、トーストが表示される
  □ 数秒後に My Page Plan セクションが [年額プラン] に切り替わる
  □ 本の追加が21冊目以降も可能になる
  □ 「請求を管理する」ボタンクリックで Stripe Customer Portal が開く
  ```

- [ ] **Step 6: Stripe テストモードから本番モードへの切り替え（リリース時）**

  ```
  1. Stripe Dashboard → テストモードをオフにして本番モードへ
  2. 本番用 API キーを Firebase Extension の設定に反映（Extensions → Reconfigure）
  3. 本番用 Stripe Price ID を index.html の firebasePricing 定数に反映
  4. Webhook の Signing Secret を本番用に更新
  5. git commit + push して自動デプロイ
  ```

---

## 自己レビュー（spec カバレッジ確認）

| Spec セクション | 対応タスク |
|--------------|-----------|
| Firebase Hosting 移行 | Task 2, 3 |
| Firebase Extension セットアップ | Task 1 |
| Firestore Security Rules（planStatus + bookCount 強制） | Task 2 |
| onSubscriptionWrite Function | Task 4, 5 |
| onPaymentWrite Function（永久プラン） | Task 4, 5 |
| 20冊到達バナー | Task 8 |
| アップグレードモーダル（3プランカード） | Task 9 |
| My Page プランセクション | Task 10 |
| Stripe Customer Portal リンク | Task 10 |
| 決済後 URL ハンドリング | Task 11 |
| GitHub Actions CI/CD | Task 3 |
| bookCount 管理（add/delete） | Task 7 |
| planStatus 初期ロード | Task 7 |
